# Example environment file for FocusFlow
# Copy this to `.env` and fill in your values for local development

# SQLite (default for local dev)
DATABASE_URL="file:./dev.db"

# Disable Next.js telemetry (privacy)
NEXT_TELEMETRY_DISABLED=1

# If you use NextAuth in future, set a strong secret here
# NEXTAUTH_SECRET=your-super-secret-key

# For production, set these as environment variables (not in .env)
# See README for details
